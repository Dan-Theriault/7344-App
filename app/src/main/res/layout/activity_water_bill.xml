<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="jasonngor.com.sustainabilitylifestylescorecard.WaterBillActivity">

    <include
        layout="@layout/content_water_bill"
        android:layout_height="62dp" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/wEntry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"/>

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="69dp"
        android:text="Water Usage"
        android:textSize="35dp"
        android:layout_marginTop="10dp"
        android:textAlignment="center"/>

    <ListView
        android:id="@+id/water_bill_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="80dp"></ListView>
</android.support.design.widget.CoordinatorLayout>
