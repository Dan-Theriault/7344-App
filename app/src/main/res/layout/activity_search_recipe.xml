<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="jasonngor.com.sustainabilitylifestylescorecard.SearchRecipeActivity"
    tools:layout_editor_absoluteY="25dp">

    <EditText
        android:id="@+id/searchRecipeTxt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="49dp"
        android:ems="10"
        android:hint="Search recipe"
        android:inputType="textPersonName"
        app:layout_constraintEnd_toStartOf="@+id/searchRecipeBtn"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/searchRecipeBtn" />

    <ListView
        android:id="@+id/recipeListView"
        android:layout_width="333dp"
        android:layout_height="368dp"
        android:layout_margin="20dp"
        android:layout_marginStart="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/searchRecipeBtn" />

    <ImageButton
        android:id="@+id/searchRecipeBtn"
        android:layout_width="45"
        android:layout_height="45"
        android:layout_marginBottom="30dp"
        android:layout_marginEnd="74dp"
        android:layout_marginTop="44dp"
        android:src="@android:drawable/ic_menu_search"
        app:layout_constraintBottom_toTopOf="@+id/recipeListView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/searchRecipeTxt"
        app:layout_constraintTop_toTopOf="parent" />

</android.support.constraint.ConstraintLayout>
